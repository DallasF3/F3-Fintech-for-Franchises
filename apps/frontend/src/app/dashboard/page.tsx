import { redirect } from "next/navigation";

export default function DashboardPage() {
  // TODO: Implement dashboard page after auth verification
  // For now, redirect back to home as placeholder
  redirect("/");
}
